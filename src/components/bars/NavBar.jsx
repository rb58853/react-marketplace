import './NavBar.css'; // Asegúrate de importar el archivo CSS que crearás en el siguiente paso

const NavBar = () => {
    return (
        <nav className="navbar">
            <div className="navbar-content">
                <a href="/home">
                    home
                </a>
            </div>
        </nav>
    );
};

export default NavBar;
